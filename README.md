## Initialize a Java Project in Repo
 - `mkdir` project-name
 - `cd` into project-name
 - `gradle init --type java-application`
 - add `id eclipse` to plugins block of the `build.gradle`
 - run `gradle eclipse`